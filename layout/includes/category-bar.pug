#category-bar
    .category-bar-items#category-bar-items
        if is_home()
            .category-bar-item#icon 
                i(class="fas fa-bookmark")
                
        !=getarray_bar("category")
    a.category-bar-more(href="/categories/") 更多